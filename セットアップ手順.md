# health-blog セットアップ手順

## ① ファイルをGitHubにアップロードする

1. https://github.com/kkazuma0415-lab/health-blog を開く
2. 「Add file」→「Upload files」をクリック
3. このフォルダの中身(README.mdを除く全部)を、フォルダ構造ごとドラッグ&ドロップ
   - `_posts`フォルダ(中身は空でOK。GitHubは空フォルダを認識しないので、
     `_posts`フォルダの中に何か1つファイルがないとアップロードされません。
     このフォルダには `.gitkeep` という空ファイルを1つ入れてあります)
   - `.github/workflows/daily-post.yml`
   - `scripts/generate_post.py`
   - `_config.yml`
   - `index.md`
   - `Gemfile`
4. 一番下の「Commit changes」をクリック

## ② APIキーをGitHub Secretsに登録する

1. リポジトリのページで「Settings」タブをクリック
2. 左メニューの「Secrets and variables」→「Actions」をクリック
3. 「New repository secret」をクリック
4. Name:`ANTHROPIC_API_KEY`
5. Secret:先ほどメモ帳に保存した `sk-ant-...` の文字列を貼り付け
6. 「Add secret」をクリック

## ③ GitHub Pagesを有効化する

1. 同じく「Settings」タブ→左メニューの「Pages」をクリック
2. 「Build and deployment」の「Source」を **「GitHub Actions」** に変更

## ④ 動作確認(手動で1回実行してみる)

1. リポジトリの「Actions」タブをクリック
2. 左側に「毎日の健康ブログ自動更新」というワークフローが表示される
3. それをクリックし、右側の「Run workflow」ボタン→もう一度「Run workflow」で手動実行
4. 1〜2分待って、緑のチェックマークが付けば成功
5. 失敗した場合は、赤い✕マークをクリックするとエラー内容が見られます。
   そのエラー画面のスクリーンショットを見せていただければ、対処法を案内します

## ⑤ 公開URLを確認する

成功すると、以下のURLでブログが公開されます。

https://kkazuma0415-lab.github.io/health-blog/

以降は毎朝7時(日本時間)に自動で記事が追加され、サイトも自動更新されます。
